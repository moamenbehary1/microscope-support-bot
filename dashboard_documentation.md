# دليل مستخدم لوحة تحكم الويب (Web Admin Dashboard)

هذا الملف يشرح بالتفصيل الممل كل جزء وكل زر وكل خاصية داخل لوحة تحكم الويب الخاصة بإدارة محتوى البوت، لكي يتمكن أي مسؤول من فهم وإدارة النظام بكل سهولة.

---

## 1. مقدمة عن لوحة التحكم
لوحة تحكم الويب هي واجهة حديثة (Modern UI) تتيح للمسؤولين (Admins) التحكم الكامل في هيكل المنهج الدراسي الذي يعرضه البوت للطلاب. بدلاً من كتابة أوامر معقدة، توفر اللوحة واجهة بصرية سهلة لإضافة وتعديل المسارات، المواد، وأنواع الملفات، بالإضافة إلى إرسال التنبيهات.

---

## 2. الدخول إلى لوحة التحكم
- يتم الوصول إلى اللوحة عبر الرابط المخصص للخادم (مثلاً `http://localhost:5000` أو الرابط الحي على Replit/Render).
- عند الدخول، يظهر واجهة أنيقة بتصميم عصري (Dark/Light themes)، وتعرض فوراً المحتوى الحالي المرتبط بقاعدة بيانات Firebase.

---

## 3. الواجهة الرئيسية (المسارات - Tracks)
تنقسم الواجهة الرئيسية إلى بطاقات (Cards) تمثل "المسارات" الدراسية.
مثال: "الفرقة الأولى"، "الفرقة الثانية".

### أ. زر إضافة مسار جديد (➕ Add Track)
- يوجد زر بارز في أعلى الصفحة لإضافة مسار جديد.
- عند الضغط عليه، تظهر نافذة منبثقة (Modal) تطلب إدخال "اسم المسار".
- بمجرد الحفظ، يظهر المسار فوراً في اللوحة كبطاقة جديدة، ويصبح متاحاً للطلاب في البوت.

### ب. إدارة المسار
داخل كل بطاقة مسار، يوجد:
1. **زر الحذف (🗑️ Delete Track):** لحذف المسار بالكامل (يجب الحذر لأن هذا سيحذف جميع المواد والملفات المرتبطة به).
2. **زر إضافة مادة (➕ Add Subject):** لإضافة مادة دراسية جديدة داخل هذا المسار المعين.

---

## 4. إدارة المواد الدراسية (Subjects)
عند الضغط على زر إضافة مادة داخل أي مسار، أو عند عرض المواد الموجودة، يمكنك التحكم في التالي:

### أ. زر إضافة مادة
- تظهر نافذة منبثقة تطلب منك إدخال "اسم المادة" (مثلاً: Anatomy).
- بعد الحفظ، تظهر المادة داخل بطاقة المسار.

### ب. أزرار التحكم في كل مادة
بجوار اسم كل مادة، توجد ثلاثة أزرار هامة جداً:

1. **زر إضافة أنواع الملفات (🏷️ Add Material Type):**
   - عند الضغط عليه، تظهر نافذة متطورة تتيح لك تحديد أنواع الملفات التي يمكن رفعها لهذه المادة.
   - **نظام الاختيار المتعدد (Multi-select Checkboxes):** يمكنك اختيار أكثر من نوع في نفس الوقت (مثل: كتب، ملخصات، امتحانات).
   - **الأنواع المخصصة (Custom Types):** إذا اخترت الخيار المسمى "Custom..."، سيظهر حقل نصي تلقائياً يطلب منك كتابة اسم النوع الجديد (مثلاً: "تسجيلات صوتية نادرة").
   - هذا الزر يوفر مرونة هائلة في تخصيص المحتوى لكل مادة على حدة.

2. **زر إرسال إشعار للمادة (📢 Broadcast):**
   - هذا الزر من أهم ميزات لوحة التحكم!
   - عند الضغط عليه، سيُطلب منك إدخال "رسالة نصية".
   - النظام سيبحث في قاعدة البيانات عن **جميع الطلاب** الذين قاموا بتصفح أو اختيار هذه المادة تحديداً في البوت.
   - سيقوم البوت بإرسال رسالتك فوراً كإشعار على تليجرام لهؤلاء الطلاب فقط (Targeted Broadcast). ممتاز لتنبيه الطلاب بمواعيد المحاضرات أو امتحانات مفاجئة.

3. **زر الحذف (🗑️ Delete):**
   - لحذف المادة بالكامل من المسار. (سيؤدي لحذف الملفات المرتبطة بها).

---

## 5. مزايا النظام الخلفي (Backend & UI Features)
- **مزامنة فورية:** أي تغيير تقوم به في لوحة التحكم (إضافة مادة، نوع، مسار) ينعكس **فوراً** على تطبيق التليجرام الخاص بالطلاب بفضل الربط المباشر مع Firebase.
- **إشعارات ذكية:** نظام الـ Broadcast مصمم بحيث يرسل الرسائل بتأخير زمني بسيط (Throttling) لتجنب حظر البوت من قِبل تيليجرام بسبب إرسال عدد هائل من الرسائل في نفس الثانية.
- **تصميم تفاعلي ومريح (Responsive):** الأزرار، حقول الاختيار (Checkboxes)، والنوافذ المنبثقة مصممة بأحدث تقنيات الـ CSS لتعطي تجربة مستخدم سلسة وعصرية (Modern UI).

---
**تم إعداد هذا الدليل لتسهيل عمل المسؤولين وتوضيح جميع الإمكانيات التي تتيحها لوحة تحكم الويب لإدارة النظام التعليمي بكل احترافية.**
